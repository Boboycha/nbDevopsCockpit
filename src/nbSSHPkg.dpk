package nbSSHPkg;

{$DESCRIPTION 'nb SSH - runtime support'}
{$RUNONLY}
{$IMPLICITBUILD ON}

requires
  rtl;

contains
  nbSSH.LibSSH2 in 'nbSSH.LibSSH2.pas',
  nbSSH.KeyValidation in 'nbSSH.KeyValidation.pas';

end.
